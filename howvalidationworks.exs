A hash that contains some data. THIS Represents
a record or record want to save to the DB
Struct
|
|           A hash that contains the properties we want to
|           update the struct with
|------------Params
\/
cast (products a changeset )
\/
validate (Adds errors to changeset, inspects props of changeset)
\/
changeset
(represents the object we are trying to update the database)


changeset- records cahnges we need to make in db
